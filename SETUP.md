# 처음 설정 — 한 번만 하면 된다

순서대로 하면 30분 안에 끝난다. **0 → 1 → 2 → 3** 까지 하면 글을 쓸 수 있고,
**4** 는 수정 제안을 스프레드시트로 받고 싶을 때만 하면 된다.

| 단계 | 무엇 | 걸리는 시간 |
|---|---|---|
| 0 | 저장소 만들고 올리기 | 5분 |
| 1 | `config.js` 에 저장소 이름 적기 | 1분 |
| 2 | GitHub 토큰 발급 | 5분 |
| 3 | 관리 화면에 들어가 시험 글 써 보기 | 3분 |
| 4 | 수정 제안 → 구글 시트 연결 | 10분 |

---

## 0. 저장소 만들고 올리기

먼저 저장소가 있어야 나머지가 의미가 있다.

1. <https://github.com/new> 로 간다.
2. **Repository name** 에 이름을 적는다. 예: `hyukjae-site`
   - 주소는 `https://<계정>.github.io/hyukjae-site/` 가 된다.
   - 주소에 저장소 이름을 안 붙이고 싶으면 이름을 **`<내계정>.github.io`** 로 지으면 된다.
     그러면 주소가 `https://<내계정>.github.io/` 가 된다.
3. **Public** 으로 만든다. (Private 은 GitHub Pages 무료 요금제에서 안 열린다)
4. "Add a README file" 같은 건 **체크하지 않는다.** 빈 저장소로 만든다.
5. **Create repository** 를 누른다.

그다음 이 폴더에서 (터미널에서 `slides-in-html-site` 폴더 안으로 들어간 뒤):

```bash
git init
git add .
git commit -m "개인 사이트"
git branch -M main
git remote add origin https://github.com/<계정>/<저장소>.git
git push -u origin main
```

마지막으로 **Pages** 를 켠다.

- 저장소 → 상단 **Settings** → 왼쪽 메뉴 **Pages**
- **Source**: `Deploy from a branch`
- **Branch**: `main` / `(root)` → **Save**
- 1~2분 뒤 화면 위쪽에 주소가 뜬다. 그 주소를 눌러 사이트가 열리는지 본다.

> **여기서 확인할 것** — 홈이 열리고, 마우스를 움직이면 배경 격자가 반응하고,
> 프로젝트·글 목록에 항목이 보이면 성공이다.
> 목록이 비어 있으면 `data/*.json` 이 안 올라간 것이다.

---

## 1. `config.js` 에 저장소 이름 적기

`assets/js/config.js` 파일을 열면 맨 위에 이렇게 돼 있다.

```js
window.SITE = {
  repo: '',
  branch: 'main',
  suggestEndpoint: '',
  ...
};
```

`repo` 에 **`계정명/저장소이름`** 을 적는다. 주소 전체가 아니라 이 두 조각만이다.

```js
  repo: 'hyukjae/hyukjae-site',
```

`suggestEndpoint` 는 4단계에서 채운다. 지금은 비워 둔다.

### 고치는 방법 두 가지

**(a) 내 컴퓨터에서 고치고 올리기**

```bash
git add assets/js/config.js
git commit -m "저장소 이름 설정"
git push
```

**(b) GitHub 웹에서 바로 고치기** — 더 간단하다

1. 저장소에서 `assets` → `js` → `config.js` 를 누른다.
2. 오른쪽 위 **연필(✏️ Edit this file)** 아이콘을 누른다.
3. `repo: ''` 를 `repo: '계정명/저장소이름'` 으로 고친다.
4. 아래 **Commit changes** → **Commit changes** 를 누른다.

> `repo` 를 안 적어도 관리 화면에서 직접 입력할 수 있다. 다만 적어 두면
> 관리 화면에 미리 채워져 나와서 편하다. **토큰은 절대 이 파일에 적지 않는다** —
> 이 파일은 누구나 볼 수 있다.

---

## 2. GitHub 토큰 발급

관리 화면이 저장소에 글을 커밋할 때 쓰는 열쇠다. **이 저장소 하나만** 만질 수 있게 좁혀서 만든다.

### 만드는 길

1. GitHub 오른쪽 위 **프로필 사진** → **Settings**
2. 왼쪽 메뉴 **맨 아래** → **Developer settings**
3. **Personal access tokens** → **Fine-grained tokens**
   (그 위의 "Tokens (classic)" 이 아니다. 반드시 **Fine-grained**)
4. 오른쪽 위 **Generate new token**

바로 가기: <https://github.com/settings/personal-access-tokens/new>

### 채울 것

| 칸 | 넣을 값 |
|---|---|
| **Token name** | `내 사이트 글쓰기` — 나중에 알아볼 이름이면 뭐든 된다 |
| **Expiration** | **90일** 정도. 짧을수록 안전하다. 만료되면 새로 만들면 된다 |
| **Resource owner** | 내 계정 |
| **Repository access** | **Only select repositories** 를 고르고, **0단계에서 만든 저장소 하나만** 선택 |
| **Permissions → Repository permissions** | 목록에서 **Contents** 를 찾아 → **Access: Read and write** |

- `Contents` 만 켜면 된다. 다른 건 전부 **No access** 로 둔다.
- `Metadata: Read-only` 는 자동으로 켜진다. 정상이다. 그대로 둔다.
- 아래 **Generate token** 을 누른다.

### 받은 다음

- `github_pat_…` 으로 시작하는 긴 문자열이 나온다.
- **이 화면을 벗어나면 다시 볼 수 없다.** 바로 복사한다.
- 어디에도 붙여넣어 저장하지 말고 **바로 3단계로 간다.**
  (굳이 보관해야 하면 비밀번호 관리자에만 넣는다. 메모장·카톡·메일은 안 된다)

> **왜 이렇게 좁히나** — 이 토큰은 브라우저에 저장된다. 혹시 새더라도
> 권한이 "이 저장소의 파일 읽기·쓰기" 하나뿐이면 피해가 이 사이트 안에서 끝난다.
> Classic 토큰은 계정 전체를 여는 열쇠라 쓰지 않는다.

---

## 3. 관리 화면 들어가기

1. 사이트 주소 뒤에 **`/admin.html`** 을 붙여 연다.
   예: `https://<계정>.github.io/<저장소>/admin.html`
   (내 컴퓨터에서 시험할 때는 `http://localhost:8080/admin.html`)
2. 세 칸을 채운다.
   - **저장소**: `계정명/저장소이름` (1단계를 했으면 이미 채워져 있다)
   - **브랜치**: `main`
   - **GitHub 토큰**: 2단계에서 복사한 `github_pat_…`
3. **확인하고 들어가기** 를 누른다.

| 나오는 말 | 뜻 |
|---|---|
| 화면이 바뀌고 "…에 씁니다" | 성공 |
| `토큰이 거부됐습니다 (401)` | 토큰이 틀렸거나 만료됨 → 다시 발급 |
| `권한이 없습니다 (403)` | Contents 권한이 Read and write 가 아님 |
| `Not Found` | 저장소 이름 오타, 또는 토큰이 이 저장소를 못 보게 돼 있음 |

### 시험 삼아 한 편 써 보기

1. **새 글** 탭에서 제목을 적으면 주소 이름이 자동으로 만들어진다.
2. 본문에 아무거나 두 줄 적고 **미리보기** 를 눌러 본다.
3. **공개** 인 채로 **저장** 을 누른다.
4. "올렸습니다" 가 뜨면 **1~2분 뒤** `/blog/` 에 가 본다. 목록에 보이면 끝이다.

> 같은 주소 이름으로 다시 저장하면 **덮어쓰기(수정)** 가 된다.
> **비공개** 로 저장한 글은 저장소로 가지 않고 이 브라우저에만 남는다.
> 오래 둘 글은 **.md 내려받기** 로 백업해 둔다.

### 다 쓰고 나서

오른쪽 위 **토큰 지우기** 를 누른다. 다음에 쓸 때 다시 붙여넣으면 된다.
**공용 컴퓨터에서는 아예 로그인하지 않는다.**

---

## 4. 수정 제안 → 구글 스프레드시트

프로젝트 페이지 맨 아래 제안 폼이 보낸 내용을 시트에 한 줄씩 쌓는다.
**이걸 안 해도 사이트는 잘 돌아간다.** 폼이 "아직 연결 전"이라고 말하고 내용 복사 버튼을 띄운다.

### 4-1. 시트와 스크립트 만들기

1. <https://sheets.new> 로 새 스프레드시트를 만든다. 이름은 아무거나 (예: `사이트 수정 제안`).
2. 상단 메뉴 **확장 프로그램 → Apps Script** 를 누른다.
3. 새 탭에 편집기가 열린다. **`Code.gs` 안의 코드를 전부 지운다.**
4. 저장소의 **`google-apps-script.gs` 파일 내용을 통째로 복사해 붙여넣는다.**
5. 💾 (저장) 을 누른다.

### 4-2. 웹앱으로 배포하기

1. 오른쪽 위 **배포 → 새 배포**
2. 왼쪽 **⚙️(유형 선택)** → **웹 앱**
3. 이렇게 고른다.
   - **설명**: 아무거나
   - **다음 사용자로 실행**: **나**
   - **액세스 권한이 있는 사용자**: **모든 사용자**
4. **배포** 를 누른다 → 권한 승인 창이 뜬다.
   - **액세스 승인** → 내 계정 선택
   - "Google에서 확인하지 않은 앱입니다" 가 뜨면 → **고급** → **…(안전하지 않음)으로 이동** → **허용**
   - (내가 만든 내 스크립트라서 나오는 경고다)
5. **웹 앱 URL** 이 나온다. `https://script.google.com/macros/s/……/exec`
   **끝이 `/exec` 인 주소**를 복사한다. (`/dev` 는 나만 쓸 수 있는 주소라 안 된다)

### 4-3. 사이트에 붙이기

`assets/js/config.js` 의 `suggestEndpoint` 에 붙여넣고 커밋한다. (1단계와 같은 방법)

```js
  suggestEndpoint: 'https://script.google.com/macros/s/AKfy……/exec',
```

### 4-4. 확인

1. 복사한 웹 앱 주소를 브라우저 주소창에 그냥 붙여넣어 열어 본다 → **`ok`** 가 나오면 정상.
2. 사이트의 프로젝트 페이지 맨 아래에서 아무 내용이나 적고 **제안 보내기**.
3. 스프레드시트를 새로고침한다. 머리글과 함께 줄이 하나 생겼으면 끝이다.

> **스크립트를 나중에 고치면** — 저장만 해서는 안 바뀐다.
> **배포 → 배포 관리 → 연필(편집) → 버전: 새 버전 → 배포** 를 해야 반영된다.
> 이때 주소는 그대로다.

---

## 자주 막히는 곳

| 증상 | 원인 · 고치는 법 |
|---|---|
| 사이트는 열리는데 목록이 비어 있다 | `data/*.json` 이 안 올라갔거나, 파일을 `file://` 로 직접 열었다. 서버(`python -m http.server`)로 열어야 한다 |
| CSS 가 하나도 안 먹는다 | `.nojekyll` 파일이 빠졌다. 저장소 루트에 있어야 한다 |
| 관리 화면에서 401 | 토큰 만료 또는 오타. 새로 발급 |
| 관리 화면에서 403 | Contents 권한이 Read-only 다. 토큰을 다시 만들거나 권한을 고친다 |
| 글을 올렸는데 사이트에 안 보인다 | Pages 배포에 1~2분 걸린다. 저장소 **Actions** 탭에서 초록 체크가 떴는지 본다 |
| 제안 폼이 "아직 연결 전" | `config.js` 의 `suggestEndpoint` 가 비었다 (4단계) |
| 제안을 보냈는데 시트에 없다 | 배포할 때 **액세스: 모든 사용자** 가 아니었거나, 주소가 `/dev` 다 |

---

## 안전 수칙 세 줄

1. 토큰은 **Fine-grained · 이 저장소 하나 · Contents 읽기쓰기** 까지만.
2. **공용 컴퓨터에서는 관리 화면에 로그인하지 않는다.** 쓰고 나면 **토큰 지우기**.
3. 토큰을 `config.js` 같은 저장소 안 파일에 절대 적지 않는다. 그 파일은 누구나 본다.
