# DESIGN.md — 문혁재 · 만든 것과 생각

이 문서는 이 웹사이트의 규칙 원본이다. `assets/css/site.css` 와 `assets/js/*` 는 여기 적힌 것을
그대로 구현한다. 값이 서로 어긋나면 **이 문서가 기준**이고, 코드를 고친다.

---

## 0. 이 사이트가 무엇인가

개인 사이트다. 만든 것(프로젝트)과 쓴 것(글)을 모아 두고, 발표할 때는 그대로 화면에 띄운다.

```
/                       홈 — 커서에 반응하는 무대 + 최근 프로젝트·글
/projects/              프로젝트 목록
/projects/slides-in-html.html   프로젝트 상세 (직접 쓴 것 — 원장 구조)
/projects/detail.html?slug=…    프로젝트 상세 (관리 화면에서 추가한 것)
/blog/                  글 목록
/blog/post.html?slug=…  글 한 편
/about.html             소개 · 이 사이트를 어떻게 만들었나
/admin.html             관리 (사이트 주인만)
```

정적 파일뿐이다. 서버도, 빌드 과정도, 프레임워크도 없다.

---

## 1. 참고 사이트에서 가져온 것 / 가져오지 않은 것

참고한 사이트는 개인 포트폴리오(`nafisazizir.com`)다.
**디자인의 "아이디어와 방법"은 저작 대상이 아니고, "표현물"은 저작 대상이다.** 그 경계로 나눴다.

### 가져온 것 — 일반적인 설계 원리 (저작 대상 아님)

| 원리 | 관찰한 값 | 이 사이트에서 |
|---|---|---|
| 완전 무채색 | 배경 순검정, 글자 near-white, 중간 회색 보조 텍스트 | 채택. 단 밝기 값은 우리 스킬 토큰을 쓴다 |
| 머리카락 선 | `1px solid rgba(255,255,255,.1)` — 면이 아니라 선으로 나눔 | 채택 (`--line`) |
| 좁은 본문 단 · 가운데 정렬 | 본문 폭 약 590–640px, 화면이 넓어져도 안 늘어남 | 채택. 본문 720px, 원장·덱만 1040px 로 넓힘 |
| 큰 제목에 음수 자간 | 60px 제목에 `letter-spacing:-3px` (≈ -0.05em) | 채택. 단 한글이라 **-0.022em** 까지만 |
| 목록을 카드가 아니라 줄로 | 테두리 없는 행 + hover 때만 배경이 옅게 뜸 | 채택 (`.item`, `.row`) |
| 짧고 일정한 전이 | `200ms cubic-bezier(.4,0,.2,1)` | 채택. 전역 `--dur`·`--ease` 로 고정 |
| 페이지를 나눔 | 홈 / 프로젝트 목록 / 프로젝트 상세 / 글 이 각각 다른 주소 | 채택 |
| **커서에 반응하는 첫 화면** | 첫 화면 전체가 캔버스이고, 마우스를 따라 화면이 변한다. 작은 점 하나가 커서를 따라다닌다 | **원리만** 채택 — 3장에 우리 방식으로 다시 만들었다 |
| 떠 있는 알약 내비 | 화면 위 중앙 고정, 라운드 22px, 배경 흐림 | **가져오지 않음** — 우리 내비는 좌우로 벌린 상단 바다 |

### 가져오지 않은 것 — 그 사이트의 표현물

- 그 사이트의 **문구·소개글·프로젝트 설명** (한 문장도 옮기지 않았다)
- **이름·로고·프로필 사진·개인 정보**
- 그 사이트의 **첫 화면 그림 자체.** 그쪽은 WebGL 셰이더로 색을 섞는다.
  이쪽은 2D 캔버스에 **점 격자**를 깔고 커서 둘레만 밝힌다. 보이는 것이 다르다
- 그 사이트의 화면 구성 전체를 그대로 재현하는 것

> 참고 흔적은 남겨 두는 편이 정직하다. 스킬 자체에도 이미 그 흔적이 있다 —
> `docs/RULES.md` 14장의 "색을 쉐이드 단계에서 고르기(Canva 의 Shade, 제작자 Nafis Azizi Riza 방식)".
> **모든 페이지 푸터에 참고 출처를 한 줄 밝힌다.** 스킬이 강제하는 출처 표기 규칙을
> 사이트가 스스로 어기면 안 된다.

---

## 2. "47번째 디자인" — 무엇을 버리고 무엇을 골랐나

요구는 *"처음 생각나는 디자인 말고, 47번째에 만들 것 같은 디자인"* 이었다.
버린 것을 적어 두는 것이 고른 것을 설명하는 가장 빠른 방법이다.

| # | 안 | 버린 이유 |
|---|---|---|
| 1 | 검정 배경 + 큰 히어로 문장 + 기능 카드 3열 + 타임라인 + 푸터 | 참고 사이트의 화면 구성을 그대로 옮긴 것. 게다가 **모든 랜딩 페이지가 이렇게 생겼다** |
| 4 | 슬라이드 스킬이니 사이트도 좌우 넘기는 슬라이드로 | 이미 덱이 있다. 사이트까지 슬라이드면 **덱을 볼 이유가 없어진다** |
| 9 | 기능마다 큰 스크린샷을 붙인 제품 소개 페이지 | 스크린샷이 주인공이 되면 "왜 이렇게 만들었나"가 뒤로 밀린다. 이 과제의 핵심은 기능이 아니라 **판단**이다 |
| 17 | 터미널 흉내(모노스페이스·커서 깜빡임·ASCII) | 개발자스러움을 연출로 쓰는 것. 스킬의 규칙("아이콘 쓰지 않음", "장식 금지")과 어긋난다 |
| 23 | 좌우 스크롤 타임라인으로 제작 과정 보여주기 | 결정 이력 66건이 가로로 늘어지면 **비교가 불가능**하다 |
| 31 | GitHub README 스타일 문서 페이지 | 정직하지만 디자인이 없다. 발표용으로 쓰기엔 화면이 심심하다 |
| 38 | 밝은 종이 톤 + 세리프 | 예쁘지만 스킬의 기본값(무채색·Pretendard·웨이트 600)과 다른 언어를 쓴다 |
| 44 | 왼쪽 고정 레일 + 오른쪽 본문 (1차 구현) | 실제로 만들어 보니 **본문이 화면 오른쪽으로 쏠려** 읽기 불편했다. 절 번호를 세우려다 중앙 정렬을 잃었다 |
| **47** | **가운데 정렬 + 커서에 반응하는 무대 + 규칙과 검사를 나란히 놓는 원장(ledger)** | 채택 |

### 47번안이 무엇인가

1. **가운데 한 단.** 모든 페이지가 같은 폭(720px)으로 가운데 정렬된다.
   표와 덱만 1040px 로 좌우로 번져 나간다(`.bleed`). 화면이 넓어져도 글줄은 안 길어진다.
2. **원장(ledger) 구조.** 화면의 기본 단위가 카드가 아니라 **표의 행**이다.
   왼쪽에 "정한 것", 오른쪽에 "왜 / 무엇이 그것을 지키는가".
   결정 66건, 검사 7종, 남은 문제 13건이 전부 같은 행 구조를 쓴다.
3. **사이트가 스킬의 규칙을 그대로 지킨다.** 무채색, 웨이트 600, 선·그림자 대신 면과 라운드,
   아이콘 없음, 짧은 페이드인 하나, `word-break:keep-all`, WCAG 대비 기준.
   스킬 템플릿의 색 토큰 값을 **그대로 가져다 쓴다**. 사이트가 곧 스킬의 사용례다.
4. **자기 검사(self-check) 푸터.** 프로젝트 상세 페이지 맨 아래에서 `check.mjs` 의 대비 계산을
   브라우저에서 다시 돌려, **이 페이지 자신의 대비 결과**를 같은 형식으로 출력한다.
   말로 "대비를 지켰습니다" 하지 않고 화면에서 계산해 보인다.

### 발표 도구로서

- **발표 모드(`P`)** — `<body data-present="on">` 인 페이지에서 진행 막대와 `03 / 09` 표시가 뜨고,
  `←` `→` 로 절 단위 이동. 스크롤을 막지 않으므로 중간에 자유롭게 되돌아갈 수 있다.
- **프로젝트 상세 05절에 덱을 16:9 로 품는다.** 누르기 전에는 iframe 을 만들지 않는다.

---

## 3. 홈 무대 — 커서에 반응하는 격자

첫 화면 전체를 `<canvas>` 가 덮는다. 그 위에 눈금(점)을 깔고 **커서 가까운 눈금만 밝아진다.**

| 값 | 설정 | 이유 |
|---|---|---|
| 눈금 간격 | 30px | 촘촘하면 노이즈, 넓으면 격자가 안 읽힌다 |
| 밝히는 반경 | 210px | 문장 한 덩어리 정도. 화면 전체가 밝아지면 효과가 사라진다 |
| 평소 밝기 | `alpha .055` | 있는 듯 없는 듯. 글을 방해하면 안 된다 |
| 커서 밑 밝기 | `alpha .62` | 여기까지만. 1.0 이면 점이 글자보다 세진다 |
| 밝기 곡선 | 거리비의 **제곱** | 선형이면 뭉개진다. 제곱이면 초점이 또렷하다 |
| 따라오는 속도 | 매 프레임 9% | 즉시 따라오면 신경질적이고, 느리면 반응이 없어 보인다 |
| 색 | `--field` (글자색의 rgb 성분) | **색을 새로 지어내지 않는다.** 무채색 규칙을 그대로 지킨다 |

- 마우스가 **아직 안 움직였으면** 초점이 스스로 천천히 떠다닌다(리사주 곡선).
  한 번이라도 움직이면 자동 운동을 끄고 커서만 따라간다.
- 초점이 멈추면 `requestAnimationFrame` 을 **멈춘다.** 가만히 있는 화면이 배터리를 먹지 않는다.
- 첫 한 장은 rAF 를 기다리지 않고 **바로 그린다.** 탭이 뒤에 있어도 격자가 비어 보이지 않는다.
- 손가락 기기(`pointer:coarse`)와 `prefers-reduced-motion` 에서는 **정지된 격자 한 장**만 그린다.
- 커서를 따라다니는 **6px 점**은 별도 요소다. 링크·버튼 위에서 14px 로 커지고 옅어진다.
  마우스가 없는 기기에서는 아예 만들지 않는다.

---

## 4. 색

**스킬 템플릿(`assets/template.html`)의 무채색 기본 토큰을 그대로 쓴다.** 사이트에서 색을 새로
지어내면, "브랜드 색을 모르면 무채색" 이라는 스킬의 규칙을 사이트가 어기는 것이 된다.

### 다크(기본)

```
--bg      #101114    바탕
--panel   #1b1d22    면 (패널·코드·인용·덱)
--text    #f2f3f5    본문
--soft    #a8adb6    보조 텍스트          (bg 대비 8.4:1)
--faint   #8b909a    라벨·캡션            (bg 대비 5.9:1)
--line    #2a2d33    머리카락 선
--tint    rgba(242,243,245,.045)   hover 배경
--field   242,243,245              홈 격자 점 (rgb 성분만)
```

### 라이트

```
--bg #ffffff  --panel #f4f6fa  --text #121314
--soft #5b5f66  --faint #6a6e75  --line #e4e7ee
--tint rgba(18,19,20,.035)  --field 18,19,20
```

### 배분 65 : 25 : 10

무채색이라 "색"이 아니라 **면의 밝기**로 배분한다.

- **주조 65%** — `--bg` 위의 `--text`. 화면 대부분
- **보조 25%** — `--panel` 면. 패널, 코드 블록, 인용, 덱 재생기, 입력칸
- **강조 10%** — **반전 면**(`--text` 배경 + `--bg` 글자). 한 화면에 **최대 하나**.
  1차 버튼, 켜진 필터 칩, 반전 비교 패널에만 쓴다

### 대비 기준 (WCAG, 스킬과 동일)

- 본문 4.5:1 이상 / 큰 글자(24px+ 또는 18.66px+ bold) 3:1 이상
- 프로젝트 상세 푸터의 자기 검사가 **12곳을 실제로 계산해** 보여 준다.
  2026-08-28 기준 라이트·다크 양쪽 모두 `contrast(글자 대비 미달): []`

---

## 5. 타이포

```
--font: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont,
        "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif;
--mono: ui-monospace, SFMono-Regular, "JetBrains Mono", Consolas, Menlo, monospace;
```

- Pretendard 는 CDN(jsdelivr)의 **동적 서브셋**으로 받는다. 한글 전체는 1MB 가 넘지만
  서브셋은 실제로 쓰인 글자만 받아 수십 KB 다
- `--mono` 는 **웹폰트를 쓰지 않는다.** 번호·수치·검사 출력에만 쓰이므로 시스템 서체로 충분하다

| 자리 | 크기 | 웨이트 | 자간 |
|---|---|---|---|
| 홈 제목 | `clamp(32px, 5.2vw, 54px)` | 600 | -0.022em |
| 페이지 제목 | `clamp(28px, 4vw, 42px)` | 600 | -0.02em |
| 절 제목 | `clamp(24px, 3.2vw, 34px)` | 600 | -0.015em |
| 본문 | 17px / 행간 1.72 | 400 | normal |
| 글 본문(`.prose p`) | 17px / 행간 1.82 | 400 | normal |
| 목록 설명 | 15.5px | 400 | normal |
| 라벨·캡션 | 13px | 500 | 0.02em |

- **제목 웨이트는 600 을 넘기지 않는다** (스킬 규칙과 동일)
- 모든 글에 `word-break:keep-all; overflow-wrap:break-word` — 한글이 어절 중간에서 안 끊긴다
- 강조 밑줄은 `::after` 가 아니라 `text-decoration` 으로 긋는다 — 줄이 바뀌어도 따라간다

---

## 6. 간격 · 형태 · 레이아웃

```
간격 스케일: 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 (px)
라운드:      --r 20px (면·패널)   --r-sm 12px (행·입력칸)
본문 단:     --col 720px          넓은 것: --col-wide 1040px
좌우 여백:   --pad 24px (640px 아래에서 20px)
```

- **그림자를 쓰지 않는다.** 영역은 `--panel` 면과 라운드로 나눈다
- 테두리는 `1px solid var(--line)` 까지만. 점선·이중선을 쓰지 않는다
- **아이콘을 쓰지 않는다.** 화살표(`→`)와 번호까지만. 이모지를 쓰지 않는다
- 모든 페이지는 `.wrap` 하나로 **가운데 정렬**된다. 고정 레일을 두지 않는다(2장 44번안)
- 표·덱만 `.bleed` 로 1040px 까지 좌우로 번진다. `.bleed` 는 `width` 를 따로 정한
  컴포넌트보다 **뒤에** 와야 이긴다 (site.css 14장 바로 앞)

| 폭 | 배치 |
|---|---|
| ≥ 900px | 본문 720px 가운데, 넓은 것 1040px 가운데 |
| < 900px | 2단(`.two`,`.vs`)이 1단으로, 원장 행이 2열로 접힘 |
| < 640px | 여백 20px, 절 간격 52px, 내비 링크 축약, 커서 점 없음 |

**가로 스크롤이 절대 생기면 안 된다.** 넓은 표·코드는 `overflow-x:auto` 컨테이너가 대신 스크롤한다.

---

## 7. 모션

```
--dur: .2s   --dur-slow: .32s   --ease: cubic-bezier(.4, 0, .2, 1)
```

- 쓰는 모션은 **세 개뿐**이다. ① hover/focus 전이(.2s) ② 절이 화면에 들어올 때
  페이드인 + 8px 올라오기(.32s) ③ 홈 격자와 커서 점의 따라오기
- ②는 **숨겼다가 보이기가 아니라 보이는 것을 한 번 띄우기**다. `@keyframes rise` 를
  `.sec.in` 에만 건다. JS 가 죽어도 내용은 처음부터 보인다
- 시차(stagger)·패럴랙스·확대축소·회전을 쓰지 않는다
- `prefers-reduced-motion: reduce` 에서 **전부 끈다**. 커서 점은 만들지도 않는다

---

## 8. 접근성

- 모든 대화형 요소에 보이는 포커스 링: `outline:2px solid var(--text); outline-offset:3px`
- 각 페이지 첫 요소가 "본문으로 건너뛰기"
- 절은 `<section aria-labelledby>`, 필터는 `aria-pressed`, 탭은 `role="tab"` + `aria-selected`
- 아코디언은 `<details>/<summary>` — JS 없이도 열린다
- 색만으로 뜻을 전하지 않는다. 결정 이력의 유형은 색이 아니라 **글자 뱃지**로 구분한다
- 폼의 모든 입력칸에 `<label>`. 상태 메시지는 `role="status"`
- `<html lang="ko">`, 캔버스는 `aria-hidden`

---

## 9. 데이터와 주소

내용은 **파일**로 있다. 데이터베이스가 없다.

```
data/projects.json     프로젝트 목록 (slug·제목·날짜·요약·꼬리표·상태·page)
data/posts.json        글 목록
content/projects/*.md  프로젝트 본문
content/posts/*.md     글 본문
```

- 목록은 `assets/js/lists.js` 가 그린다. `data-list="projects|posts"` 만 붙이면 된다
- 글·프로젝트 한 편은 `assets/js/entry.js` 가 그린다
- 마크다운 변환기(`assets/js/content.js`)는 **직접 만들었다.** 외부 스크립트를 하나도
  안 부른다는 규칙(11장)을 지키기 위해서다. 지원 문법은 제목·굵게·기울임·코드·링크·
  목록·번호목록·인용·구분선·코드블록까지다
- 주소는 `post.html?slug=…` 형식이다. 정적 호스팅에서는 글마다 폴더를 **미리** 만들 수 없고,
  관리 화면에서 글을 추가할 때 새 HTML 파일까지 만들면 되돌리기 어려워진다
- `projects.json` 의 `page` 가 있으면 그 HTML 로, 없으면 `detail.html?slug=` 로 간다.
  손으로 공들여 쓴 프로젝트와 관리 화면에서 올린 프로젝트가 한 목록에 섞여도 된다

---

## 10. 관리 화면과 보안 (`/admin.html`)

**이 사이트에는 서버가 없다.** 그래서 로그인을 만들 수 없다 —
정적 사이트에서 자바스크립트로 검사하는 비밀번호는 소스만 열면 보인다.

대신 **GitHub 개인 토큰**을 로그인 대신 쓴다.

| 규칙 | 이유 |
|---|---|
| 토큰은 `localStorage` 에만 둔다 | 이 브라우저 밖으로 안 나간다. 서버가 없으니 보낼 곳도 없다 |
| 토큰은 **GitHub API 로만** 보낸다 | 다른 어떤 곳으로도 전송하지 않는다 |
| Fine-grained 토큰 · 이 저장소 하나 · `Contents: Read and write` | 새더라도 피해가 이 사이트 안에서 끝난다 |
| 들어갈 때 `GET /repos/{repo}` 로 확인 | 오타·만료·권한 부족을 그 자리에서 알려 준다 |
| "토큰 지우기" 버튼 | 공용 컴퓨터에서 쓰고 나면 지운다 |
| 외부 스크립트 0개 | 이 페이지에 남의 코드가 없으니 토큰을 훔쳐 갈 코드도 없다 |
| `noindex` | 검색에 안 걸리게 한다 (보안이 아니라 정리 목적) |

저장은 GitHub Contents API 두 번이다.
① `PUT content/<종류>/<slug>.md` ② `GET data/<종류>.json` → 합치기 → `PUT`.
같은 slug 로 다시 저장하면 **덮어쓰기**(수정)가 된다.

### 비공개 글

**"비공개"는 올리지 않는 것이다.** 정적 사이트에 올린 파일은 주소만 알면 누구나 볼 수 있으므로,
사이트 위에서 지킬 수 있는 비공개는 없다. 그래서 비공개로 저장한 글은

- 저장소로 **가지 않고** `localStorage` 에만 쌓인다
- 관리 화면 "비공개 일기" 탭에서만 보인다. 열기·내려받기·지우기가 된다
- 브라우저 저장 공간을 비우면 사라진다. 그래서 **`.md` 로 내려받아 두라고 화면에 적어 둔다**

### 쓰다 만 글

30초마다, 그리고 칸을 벗어날 때 `localStorage` 에 초안을 넣어 둔다.
다시 들어오면 "이어서 쓸까요?"를 묻는다. 저장에 성공하면 초안을 지운다.

---

## 11. 수정 제안 → 구글 스프레드시트

프로젝트 상세 페이지 맨 아래에 제안 폼이 있다. 보내는 곳은 Google Apps Script 웹앱이고,
받은 내용은 스프레드시트에 한 줄씩 쌓인다. 만드는 방법은 `google-apps-script.gs` 맨 위에 있다.

- Apps Script 는 CORS 머리글을 붙여 주지 않으므로 `mode:'no-cors'` + `text/plain` 으로 보낸다.
  사전 요청(preflight)이 없어 그냥 통과한다. 대신 **응답을 읽을 수 없다** —
  그래서 화면은 "보냈습니다"까지만 말한다
- `config.js` 의 `suggestEndpoint` 가 비어 있으면 폼이 스스로 "아직 연결 전"이라고 말하고
  **내용 복사** 버튼을 띄운다. 쓴 글이 그냥 사라지지 않게
- 사람이 아닌 것이 채우는 빈 칸(honeypot)을 하나 둔다. 채워져 있으면 조용히 버린다
- 이름·연락처는 선택이다. **아무것도 안 적어도 보낼 수 있다**

---

## 12. 성능 (GitHub Pages 기준)

| 규칙 | 실제로 하는 것 |
|---|---|
| 요청 수를 줄인다 | CSS 1개, JS 3~5개, **이미지 0개**(SVG 파비콘 1개). 프레임워크·아이콘 폰트·분석 스크립트 없음 |
| JS 없이도 읽힌다 | 직접 쓴 페이지는 내용이 HTML 에 그대로 있다. JS 는 필터·발표 모드·목록 그리기만 한다 |
| 무거운 것은 미룬다 | 172KB 덱은 **누르기 전까지 iframe 을 만들지 않는다** |
| 렌더 차단을 줄인다 | CSS `<link>` 하나, JS 는 전부 `defer`, 폰트 CDN 에 `preconnect` |
| 레이아웃 이동(CLS) 0 | 덱 자리는 `aspect-ratio` 로 미리 잡는다 |
| 가만히 있으면 안 돈다 | 홈 격자는 초점이 멈추면 rAF 를 끈다 |

실측 (2026-08-28, 덱 제외)

| 페이지 | 원본 | gzip |
|---|---|---|
| 홈 (HTML+CSS+JS 5개) | 55KB | **약 19KB** |
| 프로젝트 상세 HTML | 69KB | **18KB** |
| `site.css` | 24KB | 6KB |

프로젝트 상세가 큰 것은 결정 이력 66건과 남은 문제 13건이 **전부 HTML 안에 들어 있기 때문**이다.
JSON 으로 빼면 파일은 작아지지만 요청이 늘고 JS 없이는 안 읽힌다. 지금 쪽이 낫다.

---

## 13. 이 문서와 다르게 만들어도 되는 경우

없다. 다르게 만들어야 할 이유가 생기면 **이 문서를 먼저 고친다.**
그것이 이 스킬이 처음부터 하려던 일이다.
