# DESIGN.md — Slides-in-Html 아카이브 사이트

이 문서는 이 웹사이트의 디자인 규칙 원본이다. `style.css` 는 여기 적힌 값을 그대로 구현한다.
값이 서로 어긋나면 **이 문서가 기준**이고, CSS 를 고친다.

---

## 0. 이 문서가 만들어진 순서

1. 참고 사이트(`nafisazizir.com`)에서 **저작권 대상이 아닌 것만** 골라 원리로 정리했다 → 1장
2. 그 원리로 만들 수 있는 디자인을 여러 번 갈아엎고, 최종안을 골랐다 → 2장
3. 최종안을 색·타이포·간격·모션의 실제 값으로 확정했다 → 3장 이후

---

## 1. 참고 사이트에서 가져온 것 / 가져오지 않은 것

참고한 사이트는 개인 포트폴리오다. **디자인의 "아이디어와 방법"은 저작 대상이 아니고, "표현물"은
저작 대상이다.** 그 경계로 나눴다.

### 가져온 것 — 일반적인 설계 원리 (저작 대상 아님)

| 원리 | 관찰한 값 | 이 사이트에서 |
|---|---|---|
| 완전 무채색 | 배경 순검정, 글자 near-white, 중간 회색 보조 텍스트 | 그대로 채택. 단 밝기 값은 우리 스킬 토큰을 쓴다 |
| 머리카락 선 | `1px solid rgba(255,255,255,.1)` — 면이 아니라 선으로 나눔 | 그대로 채택 (`--line`) |
| 좁은 본문 단 | 본문 폭 약 590–640px, 화면이 넓어져도 안 늘어남 | 채택하되 **표·원장(ledger)이 있어 720px** 로 넓힘 |
| 큰 제목에 음수 자간 | 60px 제목에 `letter-spacing:-3px` (≈ -0.05em) | 채택. 단 한글이라 **-0.02em** 까지만 (한글은 음수 자간에 훨씬 빨리 뭉갠다) |
| 목록을 카드가 아니라 줄로 | 테두리 없는 행 + hover 때만 배경이 옅게 뜸 | 채택. `.row:hover{background:var(--tint)}` |
| 짧고 일정한 전이 | `200ms cubic-bezier(.4,0,.2,1)` | 채택. 전역 `--ease`·`--dur` 로 고정 |
| 떠 있는 알약 내비 | 화면 위 중앙 고정, 라운드 22px, 배경 흐림(blur+saturate) | **가져오지 않음** — 2장 참고 |
| 여백을 색 대신 구조로 씀 | 구분선·배경색보다 빈 공간으로 덩어리를 나눔 | 채택 |

### 가져오지 않은 것 — 그 사이트의 표현물

- 그 사이트의 **문구·소개글·프로젝트 설명** (한 문장도 옮기지 않았다)
- **이름·로고·프로필 사진·개인 정보**
- 그 사이트의 **고유 화면 구성 전체를 그대로 재현하는 것** (알약 내비 + 중앙 1단 + 동일 순서)
- 그 사이트가 쓴 폰트 구매·라이선스가 필요한 서체

> 참고 흔적을 남겨 두는 편이 정직하다고 봤다. 스킬 자체에도 이미 그 흔적이 있다 —
> `RULES.md` 14장의 "색을 쉐이드 단계에서 고르기(Canva 의 Shade, 제작자 Nafis Azizi Riza 방식)".
> 이 사이트 푸터에도 **참고 출처를 한 줄 밝힌다.** 스킬이 강제하는 출처 표기 규칙을
> 사이트가 스스로 어기면 안 된다.

---

## 2. "47번째 디자인" — 무엇을 버리고 무엇을 골랐나

요구는 *"처음 생각나는 디자인 말고, 47번째에 만들 것 같은 디자인"* 이었다.
버린 것을 적어 두는 것이 고른 것을 설명하는 가장 빠른 방법이다.

| # | 안 | 버린 이유 |
|---|---|---|
| 1 | 검정 배경 + 큰 히어로 문장 + 기능 카드 3열 + 타임라인 + 푸터 | 참고 사이트의 화면 구성을 그대로 옮긴 것. 게다가 **모든 랜딩 페이지가 이렇게 생겼다** |
| 4 | 슬라이드 스킬이니 사이트도 좌우 넘기는 슬라이드로 | 이미 덱이 있다. 사이트까지 슬라이드면 **덱을 볼 이유가 없어진다** |
| 9 | 기능마다 큰 스크린샷을 붙인 제품 소개 페이지 | 스크린샷이 주인공이 되면 "왜 이렇게 만들었나" 가 뒤로 밀린다. 이 과제의 핵심은 기능이 아니라 **판단** 이다 |
| 17 | 터미널 흉내(모노스페이스·커서 깜빡임·ASCII) | 개발자스러움을 연출로 쓰는 것. 스킬의 규칙("아이콘 쓰지 않음", "장식 금지")과 정면으로 어긋난다 |
| 23 | 좌우 스크롤 타임라인으로 제작 과정을 보여주기 | 결정 이력 66건이 가로로 늘어지면 **비교가 불가능**하다. 이력의 값어치는 "훑어보며 패턴을 찾는 것" 인데 가로 스크롤이 그걸 막는다 |
| 31 | GitHub README 스타일 문서 페이지 | 정직하지만 디자인이 없다. 발표용으로 쓰기엔 화면이 심심하다 |
| 38 | 밝은 종이 톤 + 세리프 | 예쁘지만 스킬의 기본값(무채색·Pretendard·웨이트 600)과 다른 언어를 쓴다. **사이트가 스킬을 설명하면서 스킬의 규칙을 안 지키는 꼴** |
| **47** | **명세서(spec sheet) — 규칙과 그 규칙을 지키는 검사를 나란히 놓는 원장(ledger)** | 채택 |

### 47번안이 무엇인가

이 스킬의 핵심 주장은 덱 안에 한 문장으로 들어 있다.

> 디자인 규칙을 문서로 적어 두는 대신, **지켰는지 기계가 검사하게** 만들었다.

그래서 사이트의 형태를 그 주장과 같게 만든다. **사이트가 자기 주장을 자기 형태로 증명한다.**

1. **원장(ledger) 구조.** 화면의 기본 단위가 카드가 아니라 **표의 행**이다.
   왼쪽에 "정한 것", 오른쪽에 "왜 / 무엇이 그것을 지키는가". 결정 이력 66건, 검사 7종,
   남은 문제 13건이 전부 같은 행 구조를 쓴다. 훑으면서 비교할 수 있다.
2. **번호가 붙은 명세 구조.** 모든 절에 `00`–`08` 번호가 붙고, 왼쪽 고정 레일이 그 번호를
   눈금(ruler)처럼 세워 둔다. 지금 보고 있는 절만 눈금이 길어진다. 발표 중 "지금 3번입니다" 가
   화면에 그대로 있다.
3. **사이트가 스킬의 규칙을 그대로 지킨다.** 무채색, 웨이트 600, 선·그림자 대신 면과 라운드,
   아이콘 없음, 페이드인 하나, `word-break:keep-all`, WCAG 대비 기준. 스킬 템플릿의 색 토큰
   값을 **그대로 가져다 쓴다**(3장). 사이트가 곧 스킬의 사용례다.
4. **자기 검사(self-check) 푸터.** 페이지 맨 아래에서 `check.mjs` 의 대비 계산을 브라우저에서
   다시 돌려, **이 사이트 자신의 색 대비 결과**를 `check.mjs` 와 같은 형식으로 출력한다.
   말로 "대비를 지켰습니다" 하지 않고 화면에서 계산해 보인다.
5. **알약 내비를 안 쓰는 이유.** 참고 사이트는 절이 4개라 알약이 맞다. 이 사이트는 9개고
   순서가 곧 발표 순서다. 순서 있는 목록에는 **번호가 붙은 세로 레일**이 맞다.

### 발표 도구로서

이 사이트는 그대로 발표 화면이 된다.

- **발표 모드(`P` 또는 우상단 버튼)** — 진행 막대와 `03 / 08` 표시가 뜨고, `←` `→` 로 절 단위 이동.
  스크롤을 막지 않으므로 중간에 자유롭게 되돌아갈 수 있다.
- **05 절에 덱을 16:9 로 품는다.** 누르기 전에는 iframe 을 만들지 않는다(지연 로딩).
  누르면 그 자리에서 재생되고, 전체화면·새 탭도 된다.

---

## 3. 색

**스킬 템플릿(`assets/template.html`)의 무채색 기본 토큰을 그대로 쓴다.** 사이트에서 색을 새로
지어내면, "브랜드 색을 모르면 무채색" 이라는 스킬의 규칙을 사이트가 어기는 것이 된다.

### 다크(기본)

```
--bg      #101114    바탕
--panel   #1b1d22    면 (카드·코드·표 머리)
--text    #f2f3f5    본문
--soft    #a8adb6    보조 텍스트          (bg 대비 8.0:1)
--faint   #8b909a    라벨·캡션 (14px 이상) (bg 대비 5.8:1)
--line    #2a2d33    머리카락 선
--tint    rgba(242,243,245,.045)   hover 배경
--emph    #f2f3f5    본문 안 강조 (다크에서는 본문보다 밝아야 강조로 읽힌다)
```

### 라이트

```
--bg #ffffff  --panel #f4f6fa  --text #121314
--soft #5b5f66  --faint #6a6e75  --line #e4e7ee
--tint rgba(18,19,20,.035)  --emph #121314
```

### 배분 65 : 25 : 10

무채색이라 "색" 이 아니라 **면의 밝기**로 배분한다.

- **주조 65%** — `--bg` 위의 `--text`. 화면 대부분
- **보조 25%** — `--panel` 면. 표 머리, 코드 블록, 인용, 덱 재생기, 푸터
- **강조 10%** — **반전 면**(`--text` 배경 + `--bg` 글자). 한 절에 **최대 하나**.
  1차 버튼, 지금 위치 눈금, 차트 강조 막대에만 쓴다. 두 번 쓰면 이미 강조가 아니다

### 대비 기준 (WCAG, 스킬과 동일)

- 본문 4.5:1 이상 / 큰 글자(24px+ 또는 18.66px+ bold) 3:1 이상
- 위 값들은 전부 이 기준을 넘긴다. 푸터의 자기 검사가 실제로 계산해 보여 준다

---

## 4. 타이포

### 서체

```
--font: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont,
        "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif;
--mono: ui-monospace, SFMono-Regular, "JetBrains Mono", Consolas, Menlo, monospace;
```

- Pretendard 는 CDN(jsdelivr)의 **동적 서브셋**으로 받는다. 한글 전체를 받으면 1MB 가 넘는데,
  서브셋은 실제로 쓰인 글자만 받아 수십 KB 다
- `--mono` 는 **웹폰트를 쓰지 않는다.** 번호·수치·검사 출력에만 쓰이므로 시스템 서체로 충분하고,
  요청 하나를 아낀다
- 폰트가 늦게 와도 글이 안 보이면 안 되므로 `font-display:swap`, 그리고 fallback 스택을 길게 둔다

### 크기 (전부 `clamp()` — 창이 좁아지면 같이 줄어든다)

| 자리 | 크기 | 웨이트 | 자간 | 행간 |
|---|---|---|---|---|
| 표지 제목 | `clamp(32px, 5.4vw, 54px)` | 600 | -0.02em | 1.14 |
| 절 제목 | `clamp(26px, 3.4vw, 38px)` | 600 | -0.015em | 1.2 |
| 소제목 | 20px | 600 | normal | 1.35 |
| 본문 | 17px | 400 | normal | 1.72 |
| 보조 본문 | 15px | 400 | normal | 1.65 |
| 라벨·캡션 | 13px | 500 | 0.02em | 1.5 |
| 절 번호 | 13px mono | 500 | 0.06em | 1 |

- **제목 웨이트는 600 을 넘기지 않는다** (스킬 규칙과 동일). 700·800 을 쓰지 않는다
- 표지 제목의 강조 밑줄은 `::after` 가 아니라 `text-decoration` 으로 긋는다 — 어절이
  다음 줄로 넘어가도 밑줄이 각 줄에 따라간다
- 모든 글 요소에 `word-break:keep-all; overflow-wrap:break-word` — 한글이 어절 중간에서
  안 끊긴다
- 본문 단 폭 **최대 720px** (약 45–50자). 원장 표는 예외로 1040px 까지 넓힌다

---

## 5. 간격 · 형태

```
간격 스케일: 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64 · 96 · 128 (px)
라운드:      --r 20px (면·패널)   --r-sm 12px (행·버튼·뱃지)
```

- **그림자를 쓰지 않는다.** 스킬 규칙과 동일. 영역은 `--panel` 면과 라운드로 나눈다
- 테두리는 `1px solid var(--line)` 까지만. 2px 이상, 점선, 이중선을 쓰지 않는다
- **아이콘을 쓰지 않는다.** 화살표(`→`)와 번호까지만 허용한다. 이모지를 쓰지 않는다
- 절 사이 세로 간격 96px(모바일 64px), 절 안 덩어리 사이 32px

---

## 6. 레이아웃

```
┌──────────┬────────────────────────────────────────┐
│  레일     │  본문 단                                │
│ (고정)    │  max 720px (원장은 1040px)              │
│  212px   │                                        │
│  00 표지  │  ── 절 제목                             │
│  01 왜   │  본문                                   │
│ ▌02 무엇  │  ▢ 원장 행                              │
│  03 언제  │  ▢ 원장 행                              │
└──────────┴────────────────────────────────────────┘
```

| 폭 | 배치 |
|---|---|
| ≥ 1180px | 왼쪽 고정 레일 + 본문. 레일 212px, 본문 왼쪽 정렬 |
| 900–1179px | 레일 숨김. 상단에 얇은 진행 막대 + `목차` 버튼(드롭다운) |
| < 900px | 1단. 좌우 여백 20px, 원장 표는 가로 스크롤 컨테이너 안 |
| < 600px | 표지 제목 32px, 절 간격 56px |

- **가로 스크롤이 절대 생기면 안 된다.** 넓은 표는 `overflow-x:auto` 컨테이너가 대신 스크롤한다
- 덱 재생기는 `aspect-ratio:16/9` 로 비율을 지킨다

---

## 7. 모션

```
--dur:  .2s     --dur-slow: .32s
--ease: cubic-bezier(.4, 0, .2, 1)
```

- 쓰는 모션은 **두 개뿐**이다. ① hover/focus 배경·색 전이(.2s) ② 절이 화면에 들어올 때
  페이드인 + 8px 올라오기(.32s) — 스킬 덱의 슬라이드 전환과 같은 값이다
- 시차(stagger)·패럴랙스·확대축소·회전을 쓰지 않는다
- `@media (prefers-reduced-motion: reduce)` 에서 **전부 끈다** (전이 0s, 등장 애니메이션 없음).
  이 경우에도 내용은 처음부터 보이는 상태여야 한다 — 애니메이션이 꺼졌다고 글이 사라지면 안 된다

---

## 8. 접근성

- 모든 대화형 요소에 보이는 포커스 링: `outline:2px solid var(--text); outline-offset:3px`
- 레일은 `<nav>`, 절은 `<section aria-labelledby>`, 필터는 `role="group"` + `aria-pressed`
- 아코디언은 `<details>/<summary>` — JS 없이도 열린다
- 색만으로 뜻을 전하지 않는다. 결정 이력의 유형은 색이 아니라 **글자 뱃지**로 구분한다
- 이미지에 `alt`, 장식용에는 `alt=""`
- `<html lang="ko">`

---

## 9. 성능 (GitHub Pages 기준)

| 규칙 | 실제로 하는 것 |
|---|---|
| 요청 수를 줄인다 | CSS 1개, JS 1개, 이미지 0개(SVG 파비콘 1개). 아이콘 폰트·프레임워크·분석 스크립트 없음 |
| JS 없이도 읽힌다 | 모든 내용이 HTML 에 그대로 있다. JS 는 필터·발표 모드·자기 검사만 담당 |
| 무거운 것은 미룬다 | 172KB 덱은 **누르기 전까지 iframe 을 만들지 않는다** |
| 렌더 차단을 줄인다 | CSS 는 `<link>` 하나, JS 는 `defer`, 폰트 CDN 에 `preconnect` |
| 레이아웃 이동(CLS) 0 | 덱 자리는 `aspect-ratio` 로 미리 잡아 둔다. 폰트는 fallback 과 크기 차이가 적은 스택으로 |
| 캐시 | 정적 파일뿐. 파일명에 해시를 붙이지 않는 대신 배포가 잦지 않다는 것을 전제로 한다 |

실측 (2026-08-27, 덱 제외):

| 파일 | 원본 | gzip |
|---|---|---|
| `index.html` | 68KB | **17KB** |
| `style.css` | 19KB | **5KB** |
| `main.js` | 13KB | **5KB** |
| `favicon.svg` | 0.4KB | — |
| **첫 화면 합계** | 101KB | **약 28KB** |

`index.html` 이 큰 것은 결정 이력 66건과 남은 문제 13건이 **전부 HTML 안에 들어 있기 때문**이다.
JSON 으로 빼서 `fetch` 하면 파일은 작아지지만 요청이 늘고 JS 없이는 안 읽힌다. 지금 쪽이 낫다.

---

## 10. 이 문서와 다르게 만들어도 되는 경우

없다. 다르게 만들어야 할 이유가 생기면 **이 문서를 먼저 고친다.**
그것이 이 스킬이 처음부터 하려던 일이다.
